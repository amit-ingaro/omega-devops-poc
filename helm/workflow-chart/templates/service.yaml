apiVersion: v1
kind: Service
metadata:
  name: {{ include "workflow-chart.fullname" . }}
  labels:
    {{- include "workflow-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "workflow-chart.selectorLabels" . | nindent 4 }}
