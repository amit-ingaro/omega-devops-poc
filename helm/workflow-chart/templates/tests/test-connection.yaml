apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "workflow-chart.fullname" . }}-test-connection"
  labels:
    {{- include "workflow-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "workflow-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
